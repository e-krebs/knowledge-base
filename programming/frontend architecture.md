📝 [[Building future facing frontend architectures]]
[write code that runs in the browser, or code the browser runs](https://blog.jim-nielsen.com/2025/more-control-equals-less-performance/) — each bit of control you keep costs performance native apis give free
📝 [[virtual scrolling for billions of rows]]
[zero: sync engine](https://zero.rocicorp.dev/) — queries run locally against a synced postgres replica before the server confirms
